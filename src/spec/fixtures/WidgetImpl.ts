import Widget from "../../Widget";

// Widget implementation stub to test the class
class WidgetImpl extends Widget {
    findById(id: string): Widget {
        throw new Error("Not implemented");
    }
}

export default WidgetImpl;