name: ALPHA - Create PR on dependencies of this library

on:
  release:
    types: [published]
  workflow_dispatch:

jobs:
  create-pr:
    if: github.event.release.prerelease == true && contains(github.event.release.tag_name, '-alpha.')
    runs-on: ubuntu-latest

    steps:
    - name: Checkout webclient repository
      uses: actions/checkout@v2
      with:
        repository: 'gisce/react-ooui'
        ref: alpha
        token: ${{ secrets.GH_PAT }}

    - name: Update library version in package.json
      run: |
        NEW_VERSION=${{ github.event.release.tag_name }}
        NEW_VERSION=${NEW_VERSION#v}  # Remove the leading 'v'
        sed -i 's|"@gisce/ooui": "[^"]*"|"@gisce/ooui": "'"$NEW_VERSION"'"|' package.json

    - name: Setup Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '20.5.0'

    - name: Install npm dependencies and generate package-lock.json
      run: |
        npm install

    - name: Create Pull Request
      uses: peter-evans/create-pull-request@v3
      with:
        token: ${{ secrets.GH_PAT }}
        commit-message: "fix: Update @gisce/ooui to ${{ github.event.release.tag_name }}"
        title: "Update @gisce/ooui to ${{ github.event.release.tag_name }}"
        branch: "update-ooui-${{ github.event.release.tag_name }}"
